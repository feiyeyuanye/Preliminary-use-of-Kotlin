package com.example.myapplication.activity

/**
 * Activity 的生命周期
 * Android 使用任务（task）来管理 Activity，
 * 一个任务就是一组存放在栈里的 Activity 的集合，这个栈也被称作返回栈（back task）。
 * 栈是一种后进先出的数据结构。
 *
 * Activity 的状态：
 * 1. 运行状态：当其位于栈顶时。
 * 2. 暂停状态：当其不再处于栈顶，但仍然可见。（比如对话框形式的 Activity 并不会完全遮挡住下面的 Activity）
 * 3. 停止状态：当其不再处于栈顶，并且完全不可见。
 * 4. 销毁状态：当其从返回栈中移除
 */
class LifeCycleActivity {






}